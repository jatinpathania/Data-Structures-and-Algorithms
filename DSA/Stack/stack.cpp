#include <iostream>
using namespace std;
int main()
{
    //works on principle of LIFO
    //operations
    // isEmpty();
    //if(size==0)  || if(top==-1)
    // stack is empty

    // isFull();
    // if (size==capacity || if(top==capacity))

    // peek() or top();
    // check if stack is  empty
    // return stack is empty
    // otherwise return ele at top position

    // push();
    // check if stack is empty
    // if full.. return stack is full
    // otherwise top+=1
    // add element at top position
    
    // pop();
    // check stack is empty ot not
    // is stack is empty .. return stack is empty
    // return ele at top position
    // top-= 1 

    // capacity();
    // size();
return 0;
}