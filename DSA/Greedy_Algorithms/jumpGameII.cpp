#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    int jump(vector<int>& nums){
        int maxHeight=0;
        int i=0;
        while(i< nums.size()){
        }
    }
};

int main() {
    Solution sol;
    vector<int> nums = {2, 3, 1, 1, 4};
    cout << "Minimum jumps required: " << sol.jump(nums) << endl;
    return 0;
}
